# receipt_lrcnn_v1 checkpoint

Copy the trained detector checkpoint here with the exact filename:

```text
checkpoints/receipt_lrcnn_v1/best.pt
```

The checkpoint is deliberately excluded from Git. No status-style checkpoint
is loaded by this first runner.

